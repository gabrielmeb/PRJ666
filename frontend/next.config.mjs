/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["placehold.co", "avatar.iran.liara.run", "res.cloudinary.com"],
  },
  reactStrictMode: true,
};

export default nextConfig;
